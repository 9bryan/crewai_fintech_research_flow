"""
Tests for bulk_data_tools module.

Tests bulk data download and extraction tools.

Note: These tests may be skipped or mocked in CI environments
to avoid downloading large files.
"""

import json
import pytest

# Note: Bulk download tests are optional and may be skipped
# to avoid downloading large files during testing
